// String

const name1 = "Atul"
const repoCount = 50
// console.log(name+repoCount)

console.log(`Hi ${name1}, Your Repo Count is ${repoCount}`);

console.log(name1.indexOf('t'));
console.log(name1.charAt(2));

//In Substring we cannot use -ve values
//In slice we can use -ve values